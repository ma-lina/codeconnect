import express, { Request, Response } from "express";
//import { findMentor, becomeMentor } from "../controllers/mentoringController";
import jwtAuth from "../utils/jwtAuth";

const router = express.Router();

//router.post("/find", jwtAuth, findMentor);
//router.post("/become", jwtAuth, becomeMentor);
//router.get("/", jwtAuth, );
//router.delete("/", jwtAuth, );

export default router;
